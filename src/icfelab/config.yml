encoder:
    dim: 128
    num_layers: 8
    num_head: 4
    dim_feedforward: 512
training:
    dataset_ratio: [0.8, 0.1, 0.1] # Ratio for train, val and test set.
    learning_rate: 1e-04
    weight_decay: 1e-05
